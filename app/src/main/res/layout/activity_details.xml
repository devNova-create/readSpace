<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:scrollbars="vertical"
    android:fillViewport="true"
    android:clipChildren="false"
    android:clipToPadding="false"
    tools:context=".DetailsActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:orientation="vertical">

        <!-- Top Section (Header Background + Centered Cover) -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="300dp">

            <View
                android:id="@+id/view2"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/gray" />

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/materialCardView"
                android:layout_width="145dp"
                android:layout_height="225dp"
                android:layout_gravity="center"
                android:transitionName="BookTrans"
                app:cardCornerRadius="12dp"
                app:cardElevation="6dp"
                app:strokeWidth="0dp">

                <FrameLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent">

                    <ImageView
                        android:id="@+id/mBookImage"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:scaleType="centerCrop"
                        app:srcCompat="@drawable/book_4" />

                    <View
                        android:id="@+id/view"
                        android:layout_width="20dp"
                        android:layout_height="match_parent"
                        android:background="@drawable/book_handle" />
                </FrameLayout>
            </com.google.android.material.card.MaterialCardView>
        </FrameLayout>

        <!-- Title and Floating Play Button -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipChildren="false"
            android:clipToPadding="false">

            <!-- Floating Audio Play Button (Overlaps the boundary between header and title) -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/mPlayAudio"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_marginEnd="24dp"
                app:cardBackgroundColor="@color/purple_500"
                app:cardCornerRadius="100dp"
                app:cardElevation="8dp"
                app:strokeWidth="0dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent">

                <ImageView
                    android:id="@+id/imageView2"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="center"
                    app:srcCompat="@drawable/ic_headphone"
                    app:tint="@color/white" />
            </com.google.android.material.card.MaterialCardView>

            <!-- Book Title -->
            <TextView
                android:id="@+id/mBookTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="20dp"
                android:layout_marginTop="24dp"
                android:layout_marginEnd="16dp"
                android:ellipsize="end"
                android:maxLines="2"
                android:text="Rich Dad Poor Dad By Robert Kiyosaki"
                android:textColor="@color/black"
                android:textSize="22sp"
                android:textStyle="bold"
                app:layout_constraintEnd_toStartOf="@+id/mPlayAudio"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Hidden Author Name (Needed to prevent Kotlin binding crashes) -->
            <TextView
                android:id="@+id/mAuthorName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/mBookTitle" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Book Description -->
        <TextView
            android:id="@+id/mBookDesc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="20dp"
            android:fontFamily="@font/roboto"
            android:lineSpacingExtra="5dp"
            android:text="Book description goes here..."
            android:textColor="#333333"
            android:textSize="15sp" />

        <!-- Spacer to push button to the bottom when content is short -->
        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Read Book Button (Guaranteed to render at the bottom of description) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/mReadBookBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="28dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="28dp"
            android:visibility="visible"
            android:fontFamily="@font/roboto_bold"
            android:text="Read Book"
            android:textColor="#3A1E9E"
            android:textSize="16sp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            app:backgroundTint="#D2C5FC"
            app:cornerRadius="24dp"
            app:icon="@drawable/ic_read"
            app:iconGravity="textStart"
            app:iconTint="#3A1E9E"
            app:strokeWidth="0dp" />

    </LinearLayout>
</ScrollView>